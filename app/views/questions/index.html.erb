<div class="container">
  <div class="row">
    <div class="mx-auto" >

      <h3 class="text-center">お題</h3>
      <% if user_signed_in? %>
        <% @questions.each do |question| %>

          <p>
            <%= question.theme %>

            ：会員No.<%= question.user.id %>

          </p>

        <% end %>
      <% else %>
        <p>ログインしてください</p>
      <% end %>
    </div>
  </div>
</div>
